package Tarea21;

import java.util.Scanner;

	/*
	 * Pre:...
	 * Post: Haz una clase llamada Persona que siga las siguientes condiciones:
	 *Sus atributos son: nombre, edad, DNI, sexo (H hombre, M mujer), peso y altura. 
	 *Se implementarán varios constructores:
	 *	o Un constructor por defecto.
	 *  o Un constructor con el nombre, edad y sexo, el resto por defecto.
	 *	o Un constructor con todos los atributos como parámetro.
	 *Los métodos que se implementaran son:
	 *	o calcularIMC(): calculara si la persona esta en su peso ideal
	 *	 (peso en kg/(altura^2  en m)), si esta fórmula devuelve un
	 * 	  valor menor que 20, la función devuelve un -1, si devuelve
	 *	  un número entre 20 y 25 (incluidos), significa que esta por
	 * 	  debajo de su peso ideal la función devuelve un 0  y si 
	 *	  devuelve un valor mayor que 25 significa que tiene
	 * 	  sobrepeso, la función devuelve un 1.
	 *  o esMayorDeEdad(): indica si es mayor de edad, devuelve un booleano.
	 *	o comprobarSexo(String sexo): comprueba que el sexo
	 *	  introducido es correcto. Si no es correcto, será H. No será visible al exterior.
	 * 	o toString(): devuelve toda la información del objeto.
	 * 	o generaDNI(): genera un número aleatorio de 8 cifras,
	 *	  genera a partir de este su número su letra
	 *	  correspondiente. Este método será invocado cuando se construya el objeto.
	 *  o Métodos set de cada parámetro, excepto de DNI.
	 * 
	 * 
	 * 
	 */
public class Ejercicio2 {

	public static void main(String[] args) {
		

	}

}
